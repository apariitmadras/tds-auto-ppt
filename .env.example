# Optional fallback if user doesn't provide a key (demo/dev only).
# In production, users can paste their own key client-side (we never log it).
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
PORT=8080
